{
  "name": "dominoz",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run build --watch",
    "build": "bun run build:dominoz && bun run build:types",
    "build:dominoz": "bun build src/index.ts --outfile dist/dominoz.js --minify --target=browser",
    "build:types": "tsc --declaration --emitDeclarationOnly --outDir dist",
    "publish": "bun run build && npm publish --access public"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "bin": {
    "dominoz": "./dist/dominoz.js"
  },
  "version": "0.0.2",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/rudrodip/DOMinoz.git"
  },
  "author": "rudrodip",
  "keywords": [
    "dominoz",
    "dominoz.js",
    "dominoz"
  ]
}