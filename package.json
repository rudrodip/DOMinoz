{
  "name": "domino",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run build --watch",
    "build": "bun run build:domino",
    "build:domino": "bun build src/index.ts --outfile dist/index.js --minify"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}